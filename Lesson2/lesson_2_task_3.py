import math as m

side =  float (input ('Введите число:'))
def square ():

    if isinstance (side, int) or isinstance (side, float):

        return area 

    else:

        return  m.ceil (side **2)

area = side **2


print (f'Площадь квадрата: {area:.1f} ' )

